#pragma once // imports once, doesn't reimport
#include <iostream> // basic input/output
#include <glad/glad.h> // glad library, extra opengl functionality (gl additional dependancies)
#include <GLFW/glfw3.h> //glfw3, spin up a window (cross platform windowing library) 

// glm (mathematics library)
#include <glm/glm.hpp> 
#include <glm/gtc/matrix_transform.hpp>
#include <glm/gtc/type_ptr.hpp>

// standard library
#include <string>
#include <fstream>
#include <sstream>
